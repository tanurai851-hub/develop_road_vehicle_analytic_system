# CPU-only PyTorch: the default Linux wheel is the multi-GB CUDA build, which does
# not fit small/free hosting tiers. The +cpu wheels come from PyTorch's CPU index.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.1+cpu
torchvision==0.27.1+cpu

ultralytics>=8.1.0
lap>=0.5.12                     # required by the ByteTrack tracker (else auto-installs at runtime)
opencv-python-headless>=4.8.0  # headless: servers have no libGL (avoids libGL.so.1 error)
numpy>=1.24.0
pandas>=2.0.0
pyyaml>=6.0
flask>=3.0.0
mysql-connector-python>=8.2.0
pytesseract>=0.3.10
pytest>=7.4.0
